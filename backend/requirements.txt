fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy[asyncio]>=2.0
aiosqlite>=0.20
alembic>=1.13
sse-starlette>=2.1
python-multipart>=0.0.9
python-dotenv>=1.0
Pillow>=10.0
imagehash>=4.3
httpx>=0.27
aiohttp>=3.9
opencv-python>=4.9
scipy>=1.13
numpy>=1.26
huggingface-hub>=0.30,<1.0
psutil>=5.9
# ML packages — installed from system Python via --system-site-packages venv.
# If your venv was NOT created with that flag, install manually with the
# appropriate CUDA index URL, e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
#   pip install transformers open_clip_torch accelerate safetensors timm
